environment: development

server:
  port: "8080"
  host: "localhost"

database:
  host: "localhost"
  port: "5432"
  user: "postgres"
  password: "postgres"
  dbname: "cntuanyuan"
  sslmode: "disable"

jwt:
  secret: "your-secret-key-change-in-production"
  expiration: 24

